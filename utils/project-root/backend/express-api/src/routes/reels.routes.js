// Reels routes
const express = require('express');
const router = express.Router();
const controller = require('../controllers/reels.controller');

// Define reels endpoints here

module.exports = router;
